package amilosevic.and.anovinc.ferit.temperatureprediction.repository

import amilosevic.and.anovinc.ferit.temperatureprediction.api.RetrofitInstance
import amilosevic.and.anovinc.ferit.temperatureprediction.utils.Constants

class TemperaturePredictionRepo {
    suspend fun getTemperaturePrediction() {
        //return RetrofitInstance.api.predictTemperature(Constants.API_VERSION, )
    }
}